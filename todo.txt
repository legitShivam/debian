# implement whiptail integration
# integrate wsl and baremetal debian scripts
# bat configurations
# application list
python-is-python3
conda

pacstall
sudo bash -c "$(curl -fsSL https://git.io/JsADh || wget -q https://git.io/JsADh -O -)"